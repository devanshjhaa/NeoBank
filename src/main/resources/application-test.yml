spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false

  kafka:
    bootstrap-servers: dummy:9092

  redis:
    host: localhost
    port: 0
