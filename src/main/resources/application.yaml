spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/neobank?options=-c%20TimeZone=UTC
    username: neobank
    password: neobank

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.time_zone: UTC

  flyway:
    enabled: false

server:
  port: 8081
